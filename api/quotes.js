const root_url = "https://programming-quotes-api.herokuapp.com"
const all = "/quotes"
const random = "/quotes/random"

export default {
    root: root_url,
    all: root_url + all,
    random: root_url + random
}